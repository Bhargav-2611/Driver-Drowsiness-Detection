To run this code we have to install some libaries like 
      import streamlit as st
      import pandas as pd
      import cv2
      import dlib
      import numpy as np
      import pygame
      import sqlite3
      import hashlib
      import imutils
      import time
      from PIL import Image
      import base64
      from streamlit_option_menu import option_menu
 after the installation we have to create a Database to store the data .
 we cna esaily access the code . 
